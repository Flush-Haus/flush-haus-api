# Protocol Folder

Defines how the server serializes messages according to the custom poker text protocol. Includes separate subfolders for parsers (if needed) and serializers for session and game commands.